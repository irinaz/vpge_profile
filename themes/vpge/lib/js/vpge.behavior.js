
(function ($, Drupal) {

  Drupal.behaviors.vpgaTheme = {
    // Attach Drupal Behavior.
    attach(context, settings) {

    },

    // Detach Example.
    detach() {

    }
  };
})(jQuery, Drupal);
